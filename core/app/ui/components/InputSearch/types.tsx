
// Styled

import { Sizes } from '../InputField/types'

export type TOptionWrapper = {
    selected?: boolean
}

export type TArrowIcon = {
    active?: boolean
}

export type TInputSearch = {
    inputSize?: Sizes
    labelSize?: Sizes
    label?: string
    helperText?: string
}
